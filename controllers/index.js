const vocabulary = require('./vocabulary')
const users = require('./users')
const grammar = require('./grammar')
const lessons = require("./lessons")

module.exports = { users, vocabulary, grammar, lessons }
